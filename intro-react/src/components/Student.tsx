/*import Experience from "./Experiences"

type StudentProps = {
    student: {
        name: string;
        degree: string;
        points: number;
        email: string;
        experiences: { name: string }[];
}
};

export default function Student(props: StudentProps) {
    const {name, degree, points, email} = props;
    return (
        <div>
            <h1>{name}</h1>
            <p>{degree} - {points} studiepoeng</p>
            <p>Email: {email}</p>
            <h2>Erfaringer</h2>
        <div>
        {Student.experiences.map((exp) => (
            <Experience key={exp}>{exp.name}</Experience>
        ))}
        </div>
        </div>
    );
}*/